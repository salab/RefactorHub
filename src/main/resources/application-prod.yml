spring:
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false
  datasource:
    url: jdbc:${JAWSDB_URL}
  jpa:
    database: mysql
    hibernate:
      ddl-auto: create
logging:
  level:
    root: ERROR